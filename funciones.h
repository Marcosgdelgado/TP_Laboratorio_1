#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED
/** \brief Ingreso de datos por el usuario y devolucion por pantalla del resultado
 * \param numero_uno: es un float ingresado por el usuario
 * \param numero_dos: es un float ingresado por el usuario
 * \return Devuelve la suma de los variables cargadas
 *
 */

float suma(float, float);

/** \brief  Ingreso de datos por el usuario y devolucion por pantalla del resultado
 * \param numero_uno: es un float ingresado por el usuario
 * \param numero_dos: es un float ingresado por el usuario
 * \return Devuelve la resta de las variables cargadas
 *
 */

float resta(float, float);

/** \brief  Ingreso de datos por el usuario y devolucion por pantalla del resultado
 * \param  numero_uno: es un float ingresado por el usuario
  * \param numero_dos: es un float ingresado por el usuario
 * \return Devuelve la division de los datos cargados
 *
 */

float division(float, float);

/** \brief Ingreso de datos por el usuario y devolucion por pantalla del resultado
 * \param   numero_uno: es un float ingresado por el usuario
  * \param numero_dos: es un float ingresado por el usuario
 * \return Devuelve la multiplicacion de los datos cargados
 *
 */

float multiplicacion(float,float);

/** \brief Ingreso de un dato por el usuario y devolucion por pantalla del resultado
 * \param numero_uno: es un float ingresado por el usuario
 * \return Devuelve el dato factoreado
 *
 */

float factoreo(float);


#endif // FUNCIONES_H_INCLUDED
