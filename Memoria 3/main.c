#include <stdio.h>
#include <stdlib.h>

int main()
{
    int*vector;
    int*aux;

    vector=(int*)calloc(5, sizeof(int));

    if(vector == NULL){

        printf()
    }
    return 0;
}
